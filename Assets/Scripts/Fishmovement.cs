using UnityEngine;

public class FishMovement : MonoBehaviour
{
    // This component is intentionally kept empty.
    //
    // FishSchoolMovement controls the entire fish school.
    // Keeping this script prevents missing-component issues
    // in existing prefabs.

    void Update()
    {
        // Individual fish do not move independently.
    }
}